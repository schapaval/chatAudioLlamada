pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            url 'https://artifacts.zeroc.com/ice/gradle'
        }
    }
}

rootProject.name = 'chatapp'
